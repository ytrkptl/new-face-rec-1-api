setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile
    db: postgres/Dockerfile
  config:
    NODE_ENV: development
run:
  web: npm start